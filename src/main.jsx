import { useEffect, useMemo, useState } from 'react'
import { createRoot } from 'react-dom/client'
import { ArrowUpRight, Bell, BrainCircuit, CalendarDays, Check, ChevronDown, ChevronRight, CircleHelp, Code2, Flame, Grid2X2, Home, Layers3, Moon, Plus, Search, Settings, Sparkles, Sun, Target, Trophy, X } from 'lucide-react'
import './styles.css'

const intervals = [1, 3, 7, 14, 30, 60]
const topics = ['All topics', 'Arrays & Hashing', 'Two Pointers', 'Sliding Window', 'Stack', 'Binary Search', 'Linked List', 'Trees', 'Graphs', 'Dynamic Programming']
const now = new Date()
const today = `${now.getFullYear()}-${String(now.getMonth() + 1).padStart(2, '0')}-${String(now.getDate()).padStart(2, '0')}`
const displayToday = new Intl.DateTimeFormat('en', { weekday: 'long', month: 'long', day: 'numeric' }).format(new Date()).toUpperCase()
const formatDate = date => new Intl.DateTimeFormat('en', { month: 'short', day: 'numeric' }).format(new Date(`${date}T12:00:00`))
function getNextDate(repetitions) { const date = new Date(`${today}T12:00:00`); date.setDate(date.getDate() + intervals[Math.min(repetitions, intervals.length - 1)]); return date.toISOString().slice(0, 10) }

function App() {
  const [problems, setProblems] = useState([])
  const [activity, setActivity] = useState({})
  const [activePage, setActivePage] = useState('Dashboard')
  const [filter, setFilter] = useState('All topics')
  const [query, setQuery] = useState('')
  const [modalOpen, setModalOpen] = useState(false)
  const [toast, setToast] = useState('')
  const [dark, setDark] = useState(() => localStorage.getItem('recall-theme') === 'dark')

  useEffect(() => { fetch('/api/state').then(response => { if (!response.ok) throw new Error('Could not load your study data'); return response.json() }).then(data => { if (!Array.isArray(data.problems)) throw new Error('Invalid study data'); setProblems(data.problems); setActivity(data.activity ?? {}) }).catch(() => showToast('Your dashboard loaded, but saved data is temporarily unavailable.')) }, [])
  useEffect(() => { localStorage.setItem('recall-theme', dark ? 'dark' : 'light') }, [dark])
  const due = useMemo(() => problems.filter(problem => problem.repetitions > 0 && problem.nextReview && problem.nextReview <= today).sort((left, right) => left.nextReview.localeCompare(right.nextReview)).slice(0, 2), [problems])
  const todayPlan = useMemo(() => problems.filter(problem => problem.plannedDate === today && problem.status === 'new'), [problems])
  const mastered = problems.filter(problem => problem.status === 'mastered').length
  const filtered = problems.filter(problem => (filter === 'All topics' || problem.category === filter) && problem.title.toLowerCase().includes(query.toLowerCase()))

  async function updateProblem(problem, patch) {
    const updated = { ...problem, ...patch }
    setProblems(items => items.map(item => item.id === problem.id ? updated : item))
    await fetch(`/api/problems/${problem.id}`, { method: 'PATCH', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(patch) })
  }
  function showToast(message) { setToast(message); setTimeout(() => setToast(''), 3000) }
  function planProblem(problem) { updateProblem(problem, { plannedDate: problem.plannedDate === today ? null : today }); showToast(problem.plannedDate === today ? 'Removed from today’s plan.' : 'Added to today’s solve list.') }
  function markSolved(problem) { const nextReview = getNextDate(0); updateProblem(problem, { status: 'learning', repetitions: 0, plannedDate: null, solvedAt: today, nextReview }); setActivity(current => ({ ...current, [today]: (current[today] || 0) + 1 })); showToast(`Solved! Your first recall is scheduled for ${formatDate(nextReview)}.`) }
  function review(problem, quality) { const repetitions = quality === 'again' ? 0 : problem.repetitions + 1; const patch = { repetitions, status: repetitions >= 4 ? 'mastered' : 'review', nextReview: quality === 'again' ? today : getNextDate(repetitions) }; updateProblem(problem, patch); setActivity(current => ({ ...current, [today]: (current[today] || 0) + 1 })); showToast(quality === 'again' ? 'Kept in today’s queue for another pass.' : `Nice! Next review ${formatDate(patch.nextReview)}.`) }
  async function addProblem(form) { const created = await fetch('/api/problems', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(form) }).then(response => response.json()); setProblems(items => [...items, created]); setModalOpen(false); showToast('Problem added. Choose it when you are ready to solve it.') }

  return <div className={`app-shell ${dark ? 'dark' : ''}`}><Sidebar activePage={activePage} setActivePage={setActivePage}/><main><header className="topbar"><div className="mobile-brand"><BrainCircuit size={22}/><span>recall</span></div><div className="search"><Search size={18}/><input value={query} onChange={event => setQuery(event.target.value)} placeholder="Search your problems..."/><span>⌘ K</span></div><div className="top-actions"><button className="icon-button theme-toggle" onClick={() => setDark(value => !value)} aria-label="Toggle theme">{dark ? <Sun size={18}/> : <Moon size={18}/>}</button><button className="icon-button"><Bell size={19}/><i/></button><button className="avatar">AK</button></div></header>{activePage === 'Dashboard' ? <Dashboard problems={problems} due={due} todayPlan={todayPlan} mastered={mastered} review={review} planProblem={planProblem} markSolved={markSolved} setModalOpen={setModalOpen} setActivePage={setActivePage} activity={activity}/> : <ProblemsPage problems={filtered} filter={filter} setFilter={setFilter} setModalOpen={setModalOpen} review={review} planProblem={planProblem}/>}</main>{modalOpen && <AddProblem onClose={() => setModalOpen(false)} onAdd={addProblem}/>} {toast && <div className="toast"><Check size={17}/>{toast}</div>}</div>
}

function Sidebar({ activePage, setActivePage }) { const items = [[Home, 'Dashboard'], [Layers3, 'My problems'], [CalendarDays, 'Review calendar'], [Target, 'Learning path']]; return <aside className="sidebar"><div className="brand"><div className="brand-mark"><BrainCircuit size={23}/></div><span>recall<span className="brand-dot">.</span></span></div><div className="nav-label">WORKSPACE</div><nav>{items.map(([Icon, label]) => <button key={label} className={activePage === label ? 'nav-item active' : 'nav-item'} onClick={() => setActivePage(label)}><Icon size={19}/>{label}{label === 'My problems' && <b>150</b>}</button>)}</nav><div className="sidebar-bottom"><button className="nav-item"><CircleHelp size={19}/>Help center</button><button className="nav-item"><Settings size={19}/>Settings</button><div className="upgrade"><Sparkles size={18}/><div><strong>Become a master</strong><span>Build your streak daily</span></div><ChevronRight size={17}/></div></div></aside> }

function Dashboard({ problems, due, todayPlan, mastered, review, planProblem, markSolved, setModalOpen, setActivePage, activity }) { return <section className="page dashboard"><div className="greeting-row"><div><p className="eyebrow">{displayToday}</p><h1>Good morning, Akhilesh <span>✦</span></h1><p className="muted">Solve as much as you want. We’ll keep revision focused at two problems a day.</p></div><button className="primary" onClick={() => setModalOpen(true)}><Plus size={18}/>Add problem</button></div><div className="overview-grid"><div className="review-hero"><div className="hero-copy"><div className="section-kicker"><Sparkles size={16}/>YOUR DAILY FOCUS</div><h2>{due.length ? `${due.length} reviews ready` : 'Build today’s plan'}</h2><p>{due.length ? 'Your two oldest due reviews are ready. Everything else rolls forward.' : 'Choose any number of fresh problems and complete them here.'}</p><button className="dark-button" onClick={() => due[0] ? document.getElementById(`review-${due[0].id}`)?.scrollIntoView({ behavior: 'smooth', block: 'center' }) : document.getElementById('today-plan')?.scrollIntoView({ behavior: 'smooth', block: 'center' })}>{due.length ? <>Start reviewing <ArrowUpRight size={17}/></> : <>Choose problems <ArrowUpRight size={17}/></>}</button></div><div className="orbit"><div className="orbit-ring ring-one"/><div className="orbit-ring ring-two"/><div className="orbit-core"><BrainCircuit size={44}/></div><span className="float-card card-a"><Code2 size={15}/>Recall</span><span className="float-card card-b"><Trophy size={15}/>Level 8</span></div></div><StatCards mastered={mastered}/></div><div className="content-grid"><div className="due-section"><section className="today-section" id="today-plan"><div className="section-heading"><div><h2>Today’s solve list <span>{todayPlan.length}</span></h2><p>Pick any fresh problems from your library—then mark them solved.</p></div><button className="text-button" onClick={() => setActivePage('My problems')}>Choose problems <ChevronRight size={16}/></button></div><div className="problem-list">{todayPlan.length ? todayPlan.map(problem => <article className="problem-card plan-card" key={problem.id}><div className="problem-number">{String(problem.id).padStart(2, '0')}</div><div className="problem-info"><h3>{problem.title}</h3><p>{problem.category}<span>•</span><b className={`difficulty ${problem.difficulty.toLowerCase()}`}>{problem.difficulty}</b></p></div><button className="remove-plan" onClick={() => planProblem(problem)}>Remove</button><button className="solve-button" onClick={() => markSolved(problem)}><Check size={16}/>Solved today</button></article>) : <div className="empty">Nothing selected yet. Choose any new problems from your library.</div>}</div></section><section className="review-section"><div className="section-heading"><div><h2>Due for review <span>{due.length}/2</span></h2><p>Only the two oldest recalls are scheduled each day.</p></div></div><div className="problem-list">{due.length ? due.map(problem => <ProblemCard key={problem.id} problem={problem} review={review}/>) : <div className="empty">No recalls due today. Your next solved problem starts its review cycle.</div>}</div></section></div><ProgressCard problems={problems} activity={activity}/></div></section> }

function StatCards({ mastered }) { return <><div className="stat-card streak"><div className="stat-icon orange"><Flame size={21}/></div><span>Current streak</span><strong>12 <small>days</small></strong><div className="mini-bars">{[5,9,15,12,19,27,35].map((height, index) => <i key={index} className={index === 6 ? 'today' : ''} style={{height}}/>)}</div><em>Personal best: 18 days</em></div><div className="stat-card"><div className="stat-icon violet"><Trophy size={20}/></div><span>Mastered</span><strong>{mastered}<small>/ 150</small></strong><div className="progress"><i style={{width: `${Math.max(5, mastered / 150 * 100)}%`}}/></div><em>{150 - mastered} problems to go</em></div></> }
function ProblemCard({ problem, review }) { return <article className="problem-card" id={`review-${problem.id}`}><div className="problem-number">{String(problem.id).padStart(2, '0')}</div><div className="problem-info"><h3>{problem.title}{problem.url && <a href={problem.url} target="_blank" rel="noreferrer"><ArrowUpRight size={15}/></a>}</h3><p>{problem.category}<span>•</span><b className={`difficulty ${problem.difficulty.toLowerCase()}`}>{problem.difficulty}</b></p></div><div className="review-actions"><span className="due-now">Due today</span><button className="again" onClick={() => review(problem, 'again')}>Again <small>1d</small></button><button className="remember" onClick={() => review(problem, 'good')}><Check size={16}/>Remembered <small>{intervals[Math.min(problem.repetitions + 1, intervals.length - 1)]}d</small></button></div></article> }
function ProgressCard({ problems }) { const solved = problems.filter(problem => problem.solvedAt).length; const days = Array.from({ length: 84 }, (_, index) => ({ index, level: index > 67 ? (index % 7 === 0 ? 3 : index % 4 === 0 ? 2 : 1) : 0 })); return <aside className="progress-card"><div className="section-heading"><div><h2>Your progress</h2><p>Keep showing up.</p></div><button className="icon-button"><ChevronDown size={17}/></button></div><div className="mastery"><div className="mastery-ring"><span>{Math.round(solved / 150 * 100)}%</span></div><div><strong>NeetCode 150</strong><p>{solved} of 150 solved</p><div className="legend"><i/><span>Mastery score</span></div></div></div><div className="heatmap-label"><span>Activity</span><span>Last 12 weeks</span></div><div className="heatmap">{days.map(day => <i key={day.index} className={`heat-${day.level}`}/>)}</div><div className="heatmap-scale"><span>Less</span>{[0,1,2,3].map(level => <i key={level} className={`heat-${level}`}/>)}<span>More</span></div><button className="outline-button"><Grid2X2 size={17}/>View review calendar</button></aside> }
function ProblemsPage({ problems, filter, setFilter, setModalOpen, review, planProblem }) { return <section className="page problems-page"><div className="greeting-row"><div><p className="eyebrow">YOUR LIBRARY</p><h1>My problems</h1><p className="muted">Choose as many unsolved problems as you want for today. Solving starts each revision schedule.</p></div><button className="primary" onClick={() => setModalOpen(true)}><Plus size={18}/>Add problem</button></div><div className="filters">{topics.map(topic => <button key={topic} onClick={() => setFilter(topic)} className={filter === topic ? 'selected' : ''}>{topic}</button>)}</div><div className="table"><div className="table-head"><span>PROBLEM</span><span>TOPIC</span><span>STATUS</span><span>NEXT REVIEW</span><span/></div>{problems.map(problem => <div className="table-row" key={problem.id}><div><strong>{problem.title}</strong>{problem.url && <a href={problem.url} target="_blank" rel="noreferrer"><ArrowUpRight size={14}/></a>}</div><span>{problem.category}</span><span className={`status ${problem.status}`}>{problem.plannedDate === today ? 'planned today' : problem.status}</span><span>{problem.nextReview ? (problem.nextReview <= today ? 'Due today' : formatDate(problem.nextReview)) : '—'}</span>{problem.status === 'new' ? <button className={problem.plannedDate === today ? 'planned-action' : ''} onClick={() => planProblem(problem)}>{problem.plannedDate === today ? 'In today’s plan' : 'Solve today'} <ChevronRight size={15}/></button> : <button onClick={() => review(problem, 'good')}>Review <ChevronRight size={15}/></button>}</div>)}</div></section> }
function AddProblem({ onClose, onAdd }) { const [form, setForm] = useState({ title: '', category: 'Arrays & Hashing', difficulty: 'Medium', url: '' }); const set = (key, value) => setForm(current => ({ ...current, [key]: value })); function submit(event) { event.preventDefault(); if (form.title.trim()) onAdd(form) } return <div className="modal-backdrop" onMouseDown={onClose}><form className="modal" onSubmit={submit} onMouseDown={event => event.stopPropagation()}><div className="modal-title"><div><p className="eyebrow">GROW YOUR LIBRARY</p><h2>Add a problem</h2></div><button type="button" className="icon-button" onClick={onClose}><X size={19}/></button></div><label>Problem name<input autoFocus required value={form.title} onChange={event => set('title', event.target.value)} placeholder="e.g. Longest Consecutive Sequence"/></label><div className="form-grid"><label>Topic<select value={form.category} onChange={event => set('category', event.target.value)}>{topics.slice(1).map(topic => <option key={topic}>{topic}</option>)}</select></label><label>Difficulty<select value={form.difficulty} onChange={event => set('difficulty', event.target.value)}>{['Easy','Medium','Hard'].map(item => <option key={item}>{item}</option>)}</select></label></div><label>Problem link <span className="optional">optional</span><input value={form.url} onChange={event => set('url', event.target.value)} placeholder="https://leetcode.com/problems/..."/></label><button className="primary submit" type="submit"><Plus size={17}/>Add to library</button></form></div> }
createRoot(document.getElementById('root')).render(<App />)
